# 短连接平台 — 中小厂面试全量包

---

## 1. 项目定位（中小厂版）

这是一个 **前后端分离的短链接 SaaS 平台**，用户注册后可以创建短链接、管理分组、查看短链接的访问统计数据（PV/UV/UIP、浏览器、地区、设备等）。后端基于 Spring Boot 多模块架构，前端基于 Vue 3。

核心业务链路分两条：**管理链路**（用户注册 → 登录 → 创建分组 → 创建短链接 → 查看统计）和**跳转链路**（访客点击短链接 → 302 重定向到原始 URL → 异步记录访问统计）。

| 维度 | 内容 |
|------|------|
| 项目类型 | 前后端分离短链接管理平台（学习驱动的实战项目） |
| 核心业务 | 短链接创建、302 跳转、多维度访问统计 |
| 核心技术栈 | Spring Boot 3 + MyBatis-Plus + MySQL + Redis + Redisson + ShardingSphere-JDBC + Vue 3 |
| 模块拆分 | admin（用户/分组管理）、project（短链接创建/跳转/统计），另有 gateway 和 aggregation 做部署支持 |
| 我负责的模块 | `admin` + `project` 两个模块的后端接口开发，SQL 建表，本地环境搭建与前后端联调 `源码已体现` |
| 项目性质 | 学习驱动的实战项目，目标是模拟真实短链接平台的核心功能，独立完成全链路开发与联调 |
| 最值得讲的 3 个关键词 | **多模块工程化实践、Redis 综合应用、ShardingSphere 分表落地** |

---

## 2. 源码分析与诊断

### 2.1 可作为简历亮点的设计（中小厂视角重排）

以下按"中小厂面试官最看重"的顺序排列：

#### 亮点 1：统一异常处理与返回格式（工程规范能力）

- **源码位置**：
  - `admin/common/web/GlobalExceptionHandler.java` / `project/common/web/GlobalExceptionHandler.java`
  - `admin/common/convention/result/Result.java` + `Results.java`
  - `admin/common/convention/exception/` 下 AbstractException / ClientException / ServiceException / RemoteException
  - `admin/common/convention/errorcode/BaseErrorCode.java` — 三级错误码体系（A 客户端错误 / B 服务端错误 / C 远程调用错误）
- **为什么中小厂看重**：统一返回格式 `{code, message, data}` + 统一异常处理 = 团队协作的基本规范。中小厂最怕招来的人接口返回格式随心所欲。
- **面试怎么讲**：所有接口返回 `Result<T>` 统一格式，业务异常通过自定义异常类抛出（ClientException / ServiceException / RemoteException），由 GlobalExceptionHandler 统一拦截转换。错误码按 A/B/C 分层，前端可以根据 code 前缀快速判断错误来源。
- **标注**：`源码已体现`

#### 亮点 2：Redis 综合实战（登录态、缓存、分布式锁）

- **源码位置**：
  - `admin/service/impl/UserServiceImpl.java` — 登录态存 Redis Hash，TTL 30 分钟；注册用 `RedissonClient.getLock()` 防重复
  - `project/service/impl/ShortLinkServiceImpl.java` — 跳转缓存查询；更新短链接时读写锁控制并发
  - `admin/common/constant/RedisCacheConstant.java` — 统一管理 Redis Key 前缀
  - `project/common/constant/RedisKeyConstant.java` — 统一管理 Key 常量
- **为什么中小厂看重**：Redis 是后端最常用的中间件，你能用 Redis 做缓存、做锁、做登录态，三项核心场景全覆盖，去了就能干活。
- **面试怎么讲**：项目里 Redis 用了三个典型场景——登录态管理（Hash + TTL）、缓存查询（String）、分布式锁（Redisson）。Key 命名统一管理在常量类里，避免散落各处导致 Key 冲突。
- **标注**：`源码已体现`

#### 亮点 3：Maven 多模块拆分，模块边界清晰

- **源码位置**：
  - 根 `pom.xml` 包含 4 个子模块（admin、project、gateway、aggregation）
  - `admin/remote/ShortLinkActualRemoteService.java` — admin 调用 project 的接口契约
  - `project/common/biz/user/UserTransmitInterceptor.java` — 请求头中读取用户信息
- **为什么中小厂看重**：能理解模块职责划分、Maven 依赖管理，知道什么代码该放在哪个模块——这是能直接参与项目开发的基本素养。
- **面试怎么讲**：admin 模块负责用户交互，project 模块负责核心跳转。两个模块没有 Maven 依赖关系，admin 不能直接 import project 的 Service——模块边界由项目结构强制约束，防止代码随意耦合。用户信息通过请求头在模块间传递，不用重复解析。
- **标注**：`源码已体现`

#### 亮点 4：MyBatis-Plus 工程化实践

- **源码位置**：
  - `admin/config/DataBaseConfiguration.java` — 分页插件注册
  - `admin/config/MyMetaObjectHandler.java` — 自动填充 createTime / updateTime / delFlag
  - `project/config/MyMetaObjectHandler.java` — 同上
  - `project/dao/mapper/ShortLinkMapper.java` — 自定义 SQL + MyBatis XML 混合使用
- **为什么中小厂看重**：中小厂 MyBatis-Plus 是标配，分页插件、自动填充、逻辑删除是日常高频操作。
- **面试怎么讲**：分页用 MP 的 PaginationInnerInterceptor 统一处理；createTime/updateTime/delFlag 用 MetaObjectHandler 自动填充，减少业务代码和遗漏风险；复杂查询（如关联 t_link_stats_today 的分页排序）用 XML 手写 SQL。
- **标注**：`源码已体现`

#### 亮点 5：ShardingSphere 分表落地 + 不同表分片键适配不同查询场景

- **源码位置**：
  - `project/resources/shardingsphere-config-dev.yaml` — t_link 按 gid 分 16 片，t_link_goto 按 full_short_url 分 16 片
  - `admin/resources/shardingsphere-config-dev.yaml` — t_user / t_group 按 username 分 16 片
  - `resources/database/link.sql` — 完整的 16 分片建表 DDL
- **为什么中小厂看重**：大部分实习生项目根本不会碰分库分表，你能搭起来并跑通就是巨大优势。中小厂数据量一上来也要面对分表问题。
- **面试怎么讲**：用 ShardingSphere-JDBC 的 HASH_MOD 算法，用户表按 username 分片（查询时能定位到唯一分片），短链接主表 t_link 按 gid 分片（保证同分组链接在同一分片，后台分页查询不跨片），路由表 t_link_goto 按 full_short_url 分片（跳转时一次 hash 定位到唯一分片）。这是两张表各自采用不同的分片键，而非一张表有多个分片键——核心逻辑是分片键的选择完全由业务查询模式决定。建表语句是我自己写的，16 张物理表通过 ShardingSphere 逻辑表统一访问。
- **标注**：`源码已体现`

#### 亮点 6：异步统计 + 幂等消费

- **源码位置**：
  - `project/mq/producer/ShortLinkStatsSaveProducer.java` — 发消息到 Redis Stream
  - `project/mq/consumer/ShortLinkStatsSaveConsumer.java` — 消费 + 统计落库
  - `project/mq/idempotent/MessageQueueIdempotentHandler.java` — Redis setIfAbsent 幂等
  - 各 StatsMapper — `ON DUPLICATE KEY UPDATE` 原子 upsert
- **为什么中小厂看重**：体现了"异步解耦"的工程思维，不是所有请求都同步处理。幂等设计是实际生产必备的。
- **面试怎么讲**：跳转请求不需要等统计写库完成，把统计信息异步发到 Redis Stream 就返回。消费者收到后用 Redis setIfAbsent 做幂等检查，防止同一条消息重复处理。统计写入用 `ON DUPLICATE KEY UPDATE` 做原子 upsert。
- **标注**：`源码已体现`

---

### 2.2 潜在风险与问题（中小厂视角：优先关注"会不会出故障"）

#### 问题 1：配置硬编码（密码写死在 YAML） `源码已体现`
- **风险级别**：高
- **出现场景**：ShardingSphere 配置文件中的数据库密码、高德地图 API Key 直接写在 YAML 中，提交到 Git 后有泄露风险
- **短期规避**：改为 `${DB_PASSWORD:}` 环境变量占位符，本地通过 IDE 环境变量注入，`.env` 加入 `.gitignore`
- **长期方案**：接入 Nacos 配置中心（项目已集成 Nacos 依赖），敏感配置统一走 Nacos 管理

#### 问题 2：布隆过滤器重启后为空 `源码已体现`
- **风险级别**：中
- **出现场景**：服务重启后 `RBloomFilter` 内存数据丢失，所有短码查重请求穿透到 DB
- **短期规避**：重启后的首次请求触发懒加载——查询 DB 后写入布隆过滤器，前几秒性能抖动可接受
- **长期方案**：启动时通过 `ApplicationRunner` 从所有分片加载已有短码到布隆过滤器，批量写入耗时可控（100 万数据约 2-3 秒）

#### 问题 3：统计消费使用 auto-ack，异常时存在消息丢失风险 `源码已体现`
- **风险级别**：中
- **出现场景**：`ShortLinkStatsSaveConsumer` 配置了 auto-ack，消息在业务处理前就被 Redis Stream 确认。如果 `actualSaveShortLinkStats` 抛异常，消息已被 ACK 不会重新投递
- **当前已做**：幂等控制（Redis SETNX）保证同一消息重复消费不会导致数据错乱，统计写入用 `ON DUPLICATE KEY UPDATE` 也是幂等的
- **尚未解决**：幂等只保证重复消费的数据正确性，未解决消息丢失问题
- **短期规避**：消费失败时打印完整错误日志 + 原始消息内容，方便人工补偿
- **长期方案**：关闭 auto-ack，业务成功后手动 ACK；失败时不 ACK，让消息留在 Stream Pending 列表等待重新投递；超过重试阈值转死信队列

#### 问题 4：Sentinel QPS 硬编码为 1 `源码已体现`
- **风险级别**：低
- **出现场景**：`SentinelRuleConfig.java` 中 `rule.setCount(1)` 是本地调试值，生产环境需要动态调整
- **短期规避**：改为从配置文件读取 `${sentinel.qps:1}`，不同环境配不同值
- **长期方案**：Sentinel 控制台 + Nacos 持久化，运行时动态修改规则无需重启

---

### 2.3 中小厂面试最加分的 3 个重构建议

1. **参数校验完善**：当前部分接口缺少 `@Validated` 校验。补充后能展示"防用户输入非法数据"的意识
2. **接口文档**：用 Knife4j/Apifox 给所有接口生成文档——中小厂特别看重"写接口的人出文档"
3. **Docker 部署**：写个 Dockerfile + docker-compose，把 MySQL + Redis + 项目一起编排——"来了就能部署上"非常加分

---

## 3. 简历优化指导

### 3.1 项目描述（稳妥版，直接贴简历）

**项目名称**：短链接平台（ShortLink）

**项目描述**（一句话，放标题下面）：
> 基于 Spring Boot 的前后端分离短链接管理系统，支持短链接创建/跳转/多维度统计，落地多模块拆分、Redis 综合应用与 ShardingSphere 分表方案。

**个人职责**（4-5 条，每条是完整的一件事）：

1. **后端模块开发**：独立完成 admin（用户注册登录、分组管理）与 project（短链接创建、302 跳转、访问统计）两个模块的接口开发，统一使用 `Result<T>` 返回格式与自定义异常体系做全局错误处理。`源码已体现`

2. **Redis 多场景落地**：使用 Redis 实现登录 Token 管理（Hash + TTL）、短链接跳转缓存（String）、分布式锁（Redisson）与消息队列（Redis Stream），Key 集中在常量类统一管理。`源码已体现`

3. **数据库设计与分表**：编写完整建表 DDL（用户表、链接表、路由表及 6 张统计表），使用 ShardingSphere-JDBC 的 HASH_MOD 算法实现 16 分片，t_link 按 gid 路由、t_link_goto 按 short_url 路由以适配不同查询场景。`源码已体现`

4. **异步统计链路**：跳转时主链路不等待统计写入，通过 Redis Stream 异步发送统计消息，消费端使用 Redis SETNX 做幂等控制，MySQL `ON DUPLICATE KEY UPDATE` 做原子 upsert，减少并发时统计偏差。`源码已体现`

5. **前后端联调**：本地搭建 MySQL/Redis 环境，配置 Vite 代理完成前后端联调；使用 AI 辅助生成 Vue 3 前端页面（Element Plus + ECharts），手动编写路由守卫与 axios 拦截器，通过接口约定驱动前后端协作开发。`源码已体现`

### 3.2 一句话亮点（适合放简历项目标题下，备选）

> Spring Boot 多模块项目，独立完成后端接口开发，落地 Redis 缓存/锁/消息队列的多场景应用与 ShardingSphere 16 分表方案，跑通前后端全链路联调。

### 3.3 关于"数据指标"的保守表达

因为源码里没有压测数据，简历里可以这样写（不过度）：

- `可安全写入`："项目本地联调通过，支持短链接创建、跳转、统计全流程闭环"
- `建议补充后再写`："经 JMeter 压测，跳转接口在缓存命中下 QPS 达到 XXXX"（等你跑完压测再补）

---

## 4. 3-5 分钟项目介绍话术（中小厂版）

---

"我做的这个短链接项目是前后端分离的 SaaS 平台。后端我独立完成了 admin 和 project 两个核心模块的接口开发，从数据库设计、分表方案，到 Redis 综合应用、异步统计链路都自己落地。"

"项目分两个核心业务模块。admin 负责用户注册登录、分组管理、短链接的增删改查。project 是核心——短链接的创建、302 跳转、访问统计都在这里。"

"创建短链接的流程是这样的：用户在前端粘贴一个长链接、选个分组，我后端对原始 URL 做 MurmurHash 得到 hash 值，再用 Base62 编码转成 6 位短码，拼上域名返回。访客点击短链接时，我先查 Redis 缓存，有直接 302 跳转，没有就查数据库、写缓存再跳转。统计信息异步写到 Redis Stream，后台消费者慢慢落库。"

"数据库这块，短链接主表 t_link 按分组 ID 分了 16 张表，这样同一个分组下的链接都在同一张表里，后台查分页不用跨表。跳转路由用的 t_link_goto 按短链接地址分片，一次 hash 就能定位到唯一那张表。建表 SQL 是我自己写的，跑了 16 个分片 DDL。两张表分片键不同——完全由各自的查询场景决定，不是一张表有多个分片键。"

"Redis 里我用到了挺多东西——登录态存 Hash 带 30 分钟过期，短链接缓存用 String，创建短链接有并发了用 Redisson 的分布式锁控制一下，统计数据用 Redis Stream 做的异步。还有一个布隆过滤器——创建短链接的时候先过布隆，判断短码有没有冲突，布隆说'不存在'就肯定不存在，直接跳过数据库查询。"

"前端 Vue 页面是我用 AI 辅助生成的——我负责定义清楚接口的入参出参，拆成单页面 prompt 喂给 AI，生成代码后我手动检查和调整字段映射、样式和交互细节。项目是学习驱动的实战项目，我的目标是模拟真实短链接平台的核心功能，把后端日常开发的全套技术栈跑通。"

"总结一下，这个项目覆盖了 Spring Boot 多模块、MyBatis-Plus、Redis 多种用法、ShardingSphere 分表，基本就是后端日常开发的全套技术栈。"

---

## 5. 前端 Vibecoding 思路（专门一节）

面试官问"你前端怎么写的"时，按这个结构讲。

### 我的 Vibecoding 流程

**第一步：定义接口文档**

在后端 Controller 写完、联调跑通后，我明确了每个接口的：
- URL + HTTP 方法（GET/POST/PUT/DELETE）
- 请求参数字段名和类型
- 返回的 `Result<T>` 里 data 字段的结构

这个就是给 AI 的输入。

**第二步：分模块驱动 AI 生成**

我不会直接说"帮我写个前端"，而是拆成页面来：

1. **登录页**：告诉 AI "用 Vue 3 + Element Plus 做一个登录页，调 POST /api/short-link/admin/v1/user/login，请求体 `{ username, password }`，响应 `{ code, message, data: { token } }`，成功后跳转到首页"
2. **短链接管理页**：告诉 AI "做一个表格页面，调 GET 接口分页查询短链接列表，每行有编辑和删除按钮，顶部有新建按钮弹表单"
3. **统计图表页**：告诉 AI "用 ECharts 画折线图展示 PV/UV 趋势，饼图展示浏览器/OS 占比，数据从统计接口拿"

**第三步：检查和调整（体现我对生成代码的理解）**

AI 生成代码后我不是直接复制粘贴，而是逐段审查和修改：

- **检查接口 field 映射**：前端字段名必须和后端 `Result<T>` 里 data 的 JSON key 完全对应。AI 有时会编造字段名，我一律按后端实际返回的字段纠正，并在 `axios` 拦截器里统一处理 `code !== "0"` 的错误提示
- **手动写核心逻辑**：路由守卫（未登录跳转登录页）、Token 存储与请求头注入、axios 请求/响应拦截器，这些涉及全局状态和安全的逻辑我不用 AI 生成，自己写
- **调整 element-plus 细节**：表格列宽、表单校验规则、按钮 loading 状态、分页参数绑定——AI 生成的模板通常缺这些交互细节，我逐个补上
- **处理边界显示**：比如统计图表数据为空时显示"暂无数据"，表格里 null 值用 `v-if` 判断显示 "-"，AI 默认不处理这些

**第四步：配置开发环境**

```javascript
// vite.config.js 配代理 —— 这个我手动写
server: {
  proxy: {
    '/api': {
      target: 'http://127.0.0.1:8002',  // 后台接口
      changeOrigin: true
    }
  }
}
```

**面试时一句话总结**：
> "前端是 Vue 3 + Element Plus，用 AI 辅助生成页面模板——我负责定义接口契约、审查和修正字段映射、手动写路由守卫和 axios 拦截器等核心逻辑。AI 是我的代码生成加速器，不是我写完就不管的黑盒。这种模式让我一个人也能高效跑通前后端全链路。",

**如果面试官追问"你离开 AI 能写前端吗"**：
> "能。Element Plus 的常用组件（表格、表单、弹窗、分页）、Vue Router 的路由配置、axios 的拦截器封装——这些我自己手写没问题。AI 帮我省的是调样式和写重复模板的时间，但核心逻辑和架构决策是我做的。"

---

## 6. 问题预测与模拟

### 6.1 中小厂高频八股文（精简版）

#### 1. Spring Boot 启动流程？
**回答要点**：main 方法 → `SpringApplication.run()` → 创建 ApplicationContext → 自动配置（`@EnableAutoConfiguration`）→ 扫描 Bean → 启动内嵌 Tomcat → 注册所有 Controller

**一句话加分**：我的项目里 admin 和 project 是两个独立的 Spring Boot 应用，各自有自己的启动类和配置，通过 Nacos 注册后由 Gateway 统一路由。

#### 2. MyBatis-Plus 怎么用？你用了哪些功能？
**回答要点**：BaseMapper 继承后自动有 CRUD，不用写 XML；分页插件 `PaginationInnerInterceptor`；自动填充用 `MetaObjectHandler`；复杂 SQL 用 `@Select` 注解或 XML mapper

**一句话加分**：统计表的 `@Insert` 里用了 `ON DUPLICATE KEY UPDATE` 做原子 upsert，避免并发场景下统计数据被覆盖。

#### 3. Redis 怎么用的？说说你项目里用了什么数据类型？
**回答要点**：String（跳转缓存）、Hash（登录 session）、SETNX（幂等控制）、Stream（统计消息）；Redisson 的 getLock 做分布式锁；Redisson 的 RBloomFilter 做短码去重

**一句话加分**：每种场景都根据特性选了合适的数据类型——登录态用 Hash 而不是 String 是为了后续扩展字段，Stream 用了消费者组保证消息不丢。

#### 4. 分布式锁怎么实现？要注意什么？
**回答要点**：Redisson getLock → Lua 脚本原子加锁 → WatchDog 自动续期 30 秒 → 解锁用 Lua 脚本校验锁持有者

**一句话加分**：创建短链接时用分布式锁保证同一个短码不会被两个线程同时插入，但锁的粒度是短码级别的，不会影响其他短码的创建。

#### 5. SQL 优化你做过什么？
**回答要点**：分片键选择让核心查询落在单分片；统计表建了联合唯一索引支持 UPSERT；t_link 的 (full_short_url, del_time) 复合唯一索引支持唯一性校验

**一句话加分**：t_link_goto 路由表只有两个字段（gid, full_short_url），极窄表查询非常快。

#### 6. 接口防刷怎么做的？
**回答要点**：Sentinel 全局 QPS 限流 + Lua 脚本做用户级滑动窗口频率限制。先触发用户级限流（友好提示），超过全局阈值才报"系统繁忙"

**一句话加分**：Lua 脚本在 Redis 里执行，INCR + EXPIRE 是原子的，不会出现计数不准。

#### 7. Nacos 在项目里用来干什么？
**回答要点**：服务注册与发现——admin、project、gateway 各模块启动后向 Nacos 注册，Gateway 通过 Nacos 的服务列表做负载均衡路由

**一句话加分**：本地联调时 Nacos 跑在 8848 端口，每个模块配置里 `spring.cloud.nacos.discovery.server-addr: 127.0.0.1:8848`。

---

### 6.2 项目深挖类问题（中小厂视角）

#### 问题 1：你这个项目是怎么跑起来的？部署的时候需要注意什么？

**【问题来源】**：话术"本地搭建 MySQL/Redis/Nacos 环境"

**【面试官真实意图】**：考察你能不能独立搭环境。中小厂很现实，你不会搭环境就没法干活。

**【不合格回答】**："装好 IDE 直接跑就行"

**【较优示范答案】**：
"本地跑起来需要 MySQL、Redis、Nacos 三个中间件先启动，然后按顺序启 admin（8002 端口）和 project（8001 端口），gateway 是可选的——不用网关的话直接调 admin 和 project 的端口也行。配置里 ShardingSphere 连的是 `127.0.0.1:3306/link` 数据库，建表 SQL 在 resources/database/link.sql 里，直接执行就行。前端 `pnpm install` 后 `pnpm dev`，Vite 代理自动转发 `/api` 请求到后端 8002。"

---

#### 问题 2：你用的是单体架构还是微服务？模块之间怎么通信？

**【问题来源】**：简历"多模块拆分" + 话术关于四个模块的描述

**【面试官真实意图】**：考察你理解部署架构，不是看你会不会用过深的微服务理论。

**【不合格回答】**："微服务"（只说了名字没说怎么调的）

**【较优示范答案】**：
"四个 Maven 模块，但部署方式分两种。分开部署的时候，admin 和 project 是两个独立进程，admin 调用 project 通过 OpenFeign，走 HTTP。合并部署用 aggregation 模块，把两个模块的包都扫进来，变成一个进程，内部方法调用。路由都用 Nacos 做服务发现。"

---

#### 问题 3：建表语句怎么写的？16 张分片表你是怎么建的？

**【问题来源】**：简历"编写完整建表 DDL" + "16 分片"

**【面试官真实意图】**：考察你实际操作过数据库设计，不是只写 Java 代码。

**【不合格回答】**："ShardingSphere 自动建的"

**【较优示范答案】**：
"16 张分片表需要手动建——t_link_0 到 t_link_15，每张表的结构完全一样，只是表名后缀不同。ShardingSphere 做逻辑表到物理表的路由，但不负责建表。建表 SQL 在 resources/database/link.sql 里，核心字段是 id、domain、short_uri、full_short_url、origin_url、gid，索引建在 full_short_url 和 gid 上。统计表（t_link_access_stats 等）没有分片，是单表，数据量相对小。"

---

#### 问题 4：创建短链接的时候，如果同一个原始 URL 重复提交怎么办？

**【问题来源】**：话术关于创建短链接的描述

**【面试官真实意图】**：考察你对重复提交的处理——不是考你理论，是考你是不是真遇到过这个问题。

**【不合格回答】**："不会重复，因为用了布隆过滤器"

**【较优示范答案】**：
"这得分场景。同一个用户用同一个原始 URL 重复创建——后端不会阻止，因为同一个长链接可能属于不同分组、不同有效期，业务上是允许的，每次都会生成不同的 short_uri。但如果同一个 short_uri 被两个请求同时插入——分布式锁会序列化这两个请求，后一个因为 short_uri 的 MurmurHash 没变，布隆会提示可能存在，然后去 DB 查唯一索引发现冲突，会重新生成短码。所以最终不会有两个相同的 short_uri。"

---

#### 问题 5：你的前端用 AI 辅助生成，具体是怎么操作的？

**【问题来源】**：话术"前端 Vue 是 AI 辅助生成的"

**【面试官真实意图】**：考察你是不是真的会用 AI 工具提效。现在中小厂很喜欢这种"AI-native"工作方式，但需要你能讲清楚操作流程，而不是"AI帮我写的"一句带过。

**【不合格回答】**："我就把需求丢给 AI 就出来了" / "AI 写的我都不懂"

**【较优示范答案】**：
"我的流程是：先自己在后端跑通接口，用 Apifox 测试确认返回格式没问题。然后按页面拆分给 AI 发 prompt——比如'用 Vue3 + Element Plus 做一个表格页面，调这个 URL，请求参数是 xxx，返回数据长这样，支持搜索和分页'。AI 生成代码后我会检查三个地方：一是 API 路径和参数名是否跟后端对得上；二是表格和表单的字段映射是否正确；三是交互细节比如提交后刷新列表、错误提示。调完再把 axios 拦截器里统一处理错误码的逻辑补上。"

---

#### 问题 6：t_link 和 t_link_goto 都是 16 张表，数据怎么保持一致？

**【问题来源】**：简历关于分表设计的描述

**【面试官真实意图】**：考察你理解冗余数据的一致性问题。

**【不合格回答】**："一起写肯定一致" / 完全不知道这是个问题

**【较优示范答案】**：
"创建短链接的时候，t_link 和 t_link_goto 是在同一个事务里写的——两个 Mapper 的 insert 都在 createShortLink 方法里，同一个 Spring 事务。这两张表虽然物理上分在不同分片（t_link 按 gid 路由、t_link_goto 按 full_short_url 路由），但 ShardingSphere 会在同一个数据库连接里做路由，事务是覆盖这两个操作的。不过严格来说，如果两个分片在不同的数据库实例上，这里就会有分布式事务问题——当前项目用的是单库 16 分片，所以事务是安全的。"

---

#### 问题 7：你碰到过什么 bug 或者难排查的问题？

**【问题来源】**：话术整体描述

**【面试官真实意图】**：考察你是不是真正写过代码。真正写过代码的人一定有 bug 故事。

**【建议回答思路】**：
"有一个印象比较深的问题。ShardingSphere 配置里指定了 `actual-data-nodes: link.t_link_${0..15}`，但 MyBatis-Plus 的 `@TableName("t_link")` 里写的是逻辑表名。一开始我写了物理表名 `t_link_0`，ShardingSphere 按照 username 做 hash 后再拼接后缀，结果最终查的表名变成了 `t_link_0_5` 这种，总是报 'table not found'。后来才发现 ShardingSphere 的配置里逻辑表名和 `@TableName` 必须一致，逻辑表名才是对外暴露的，物理分片是 ShardingSphere 内部转发。这个问题花了我一个多小时才定位到。"

---

#### 问题 8：统计表为什么不一起分片？

**【问题来源】**：简历中关于分表的描述

**【面试官真实意图】**：考察你做分表决策时有选择性，不是所有表都无脑分。

**【不合格回答】**："数据少不用分"

**【较优示范答案】**：
"统计表（t_link_access_stats 这些）我没用分片，有几个考虑。第一，统计表的数据量是按时间维度增长的，不是按业务多维度散列的，单表用时间分区就可以。第二，统计表的查询基本都是按 full_short_url + date/hour 范围查，加上唯一索引就可以满足查询性能。第三，ShardingSphere 的广播表机制可以处理这种小表——但当前数据量下广播表的收益不大，先保持单表。"

---

## 7. 最终建议

### 最值得主打的 3 个技术点（中小厂版）

1. **全链路落地的工程能力**：从建表 DDL 到 Maven 分模块，从 Controller 到数据库，从前后端联调到本地环境搭建，你能在面试里展示"这事儿从头到尾我能干完"，而不是"我只负责写 Service"
2. **Redis 的高密度使用**：一个项目里覆盖了 String/Hash/Stream/SETNX/分布式锁/布隆过滤器，中小厂面试官会认为你 Redis 实战够用了
3. **分表方案的实际操作**：不只是"了解 ShardingSphere"，而是自己写了 16 分片 DDL、选了两个不同分片键、跑通了全链路

### 最不建议主动展开的 2 个薄弱点

1. **单元测试**：项目里只有基础测试类，缺少业务测试。面试官如果问"你写过单元测试吗"，如实回答"目前是手动联调验证，后续会补 JUnit + Mockito"
2. **CI/CD 和部署自动化**：没有 Docker 化，没有 GitHub Actions。如果被问到，直接说"目前是本地部署验证，Docker 在计划中"

### 下一步优先级（按对中小厂面试收益排序）

| 优先级 | 行动 | 预计耗时 |
|--------|------|----------|
| **P0** | 用 JMeter 对跳转接口跑一次压测，记录缓存命中/回源 QPS（简历里有个数比完全没有好太多） | 1 小时 |
| **P0** | 准备一个"我碰到的 2 个 bug 和怎么解决的"小故事（面试必考题） | 30 分钟 |
| **P1** | 给所有接口补上 Knife4j 文档（pom 加依赖 + 配置类，10 分钟的事，面试能说"我写了接口文档"） | 30 分钟 |
| **P1** | 加个 Dockerfile + docker-compose.yml（最基本的就行），简历里能写"支持 Docker 部署" | 1 小时 |
| **P2** | 整理一份 ER 图或表结构说明，面试时打开给面试官看 | 30 分钟 |
| **P2** | 写 2-3 个关键接口的单元测试（能说"我有写测试的意识"） | 1 小时 |

---

> **核心思路**：少讲原理推导，多讲你做了什么、怎么做的、遇到什么问题、怎么解决的。
